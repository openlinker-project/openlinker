=== arm throughput-lane-raised ===
window elapsed:        295s
orders completed:      900
sustained rate:        3.051 orders/s = 10983 orders/hour
oldest due-job age:    1s -> 0s over the window's second half (4% of ticks rising)
queue growth:          NO - the offered load did not exceed the system, so the sustained rate above is a FLOOR on the ceiling, not the knee. Offer more (deeper backlog or a faster poll cadence) and re-run
stub feed backlog:     min=0 max=900 (reaching 0 here is NORMAL and not starvation - the poll pump converts upstream events into queued child jobs faster than they drain, so the upstream empties while the system is busiest)
available work:        min=0 max=902 (upstream + due queue + running; min 0 means the system was IDLE for part of the window, so the achieved rate is a floor on the offered load rather than the system's ceiling)
due-queued peak:       244

stub upstream requests over the window:
{
  "GET /order/checkout-forms/:id": 900,
  "GET /order/events": 30
}
