# F2 - stock propagation latency

_generated 2026-09-06T00:02:00Z (post-hoc corrected), run group run1788652154_

_Superseded numbers, kept in git history for the record only: the version of this file this run group originally produced (2026-09-05T23:53:32Z) contained a timezone-parsing bug in the summarizer that produced nonsensical multi-hour hop figures. Root-caused and fixed (see "Corrections applied after generation" below) without a re-run: the underlying `cycles.csv` this file is built from was unaffected, only the summarizer's arithmetic was wrong._

## Conditions

- Stand: `lab` (docker-compose.lab.yml, #2854), a single developer workstation shared with two other OpenLinker docker-compose stacks (`ol-demo-fresh-*`, `openlinker-*`) - no CPU pinning, no isolation from host contention. Every figure below is subject to that.
- `guard_build` PASSED: running images and the working tree both resolve to `2c758521dfd932e9fe747c6e6ec95cde32fbc101`.
- Runner: ENABLED for this scenario (`guard_runner_state enabled`) - the whole chain is unobservable otherwise. Scheduler: OFF (`guard_scheduler_off`) - the inventory sweep is a second, competing path to the same rows and was deliberately excluded so every observed job is attributable to this scenario's own webhook-triggered writes.
- **Stand exclusivity**: this run held `guard_stand_exclusive` for its entire duration. It is also, independently, checked against the known-contaminated window: F3 (a separate scenario measuring the same stand) drove a 7,000-request webhook burst through `lab-api`/`lab-postgres` between 23:27 and 23:39 UTC on 2026-09-05, while sharing the runner/scheduler state this run also depends on. **This run's measurement window (`window_start` 23:50:15 UTC, `window_stop` 23:53:31 UTC) is entirely outside that range** - no overlap.
- 6 products x 3 cycles = 18 stock-write attempts.

## Headline finding: hop t1->t2 is EXCLUDED from this measurement, not measured as fast

This scenario force-drains the outbox after every single write (calls the module's cron controller immediately). That is a deliberate experimental choice - it isolates OpenLinker's OWN work from the shop's delivery cadence, which is the more actionable half of the chain to an operator - but it means **the t1->t2 hop below is NOT a measurement of anything a real deployment experiences; it is excluded by construction.** On a stand shaped exactly like this one, hop t2 in production is bounded below by whatever external cron interval an operator (or a hosting provider's crontab) configures for the module's cron controller - commonly minutes, not the sub-second figure this run reports for it. To reconstruct a real "shop to OpenLinker" figure from the numbers below: take Hop A + Hop C + Hop D + Hop E (+ Hop F if you also want "propagation job dispatched toward the destination", which is NOT "reached the marketplace" - see the Hop 5 section) and ADD your own PrestaShop cron interval on top. That addition is the dominant term for almost any real deployment, and this run cannot supply it: nothing on this container's crontab calls the cron controller at all (`crontab -l` is empty), and the module's own response-flush fast path (#2624), which WOULD close this gap automatically, never fires on this image - its SAPI is `apache2handler`/mod_php, and `fastcgi_finish_request()` does not exist there (confirmed live via a throwaway PHP probe served over a real HTTP request: `PHP_SAPI` reports `apache2handler`, `function_exists('fastcgi_finish_request')` reports `false`). **If a deployment runs behind php-fpm instead, hop t2 collapses toward zero automatically via the fast path** - that is a real, actionable, deployment-shape-dependent fact this stand happens to be positioned to demonstrate, precisely because it is NOT running php-fpm.

## Why the earlier 2-of-25 trial undercounted (root-caused, not guessed)

Two independent, verified mechanisms explain it:

1. **The PrestaShop REST webservice cannot fire the hook at all on this PrestaShop version.** `stock_availables` PUT is dispatched generically onto `ObjectModel::update()` (`classes/webservice/WebserviceRequest.php:332`), never through the static `StockAvailable::setQuantity()` helper that is the only call site of `Hook::exec('actionUpdateQuantity', ...)` in PrestaShop 9.0.2 core (`classes/stock/StockAvailable.php:454-455`). Verified live on this stand: a real webservice `PUT` with a genuinely changed quantity returns HTTP 200 and inserts zero outbox rows.
2. **A dedup key survives until the row is CLAIMED, not until it is delivered.** `OutboxRepository::enqueueEvent` derives `dedup_key` from `(provider, connectionId, eventType, objectType, externalId)` - it does not include the timestamp or the new quantity - and `INSERT IGNORE` collides on it. The key is cleared only when the drainer *claims* a row (moves it to `processing`), not when it is delivered. Verified live: two `StockAvailable::setQuantity()` calls for the SAME product id, made before any drain, produced exactly ONE outbox row; the second call silently no-opped. With only 6 distinct products on this catalogue, a burst of writes made faster than the drain cadence collapses onto at most 6 rows - independent of how many individual stock_available rows (combinations) were actually touched.

This scenario avoids both: it drives t0 through `StockAvailable::setQuantity()` directly (drivers/ps-set-quantity.php), and it drains after every single write, so every cycle claims its own row before the next write for that product can be silently swallowed.

## Root-caused: `drain_http_status` reads 500 on every cycle - a real module bug, separable from a stand condition, and the delivery happens anyway

Two separate facts, kept separate:

- **The module bug**: `controllers/front/cron.php`'s SUCCESS branch never calls `exit` after `echo json_encode($stats)` - every ERROR branch in the same file does. So on ANY shop, a successful drain falls through into PrestaShop's ordinary front-controller page-render pipeline behind the JSON body, meaning the true response is "JSON, followed by an entire rendered HTML page" - which breaks any strict JSON-only parser on the calling side, independent of this stand.
- **The stand condition**: on this container specifically, that unwanted render throws a fatal (`MatthiasMullie\Minify\Exceptions\IOException: The file "/var/www/html/themes/classic/assets/cache/theme-3f744e.css" could not be opened for writing`, a theme asset-cache directory that is not writable), which is what turns the module bug's harmless-on-a-healthy-shop extra HTML into an HTTP 500. Confirmed by matching the Apache error-log timestamp to the exact same request's access-log line and to the outbox row's own `delivered_at` - same request, same second.

**The delivery is real regardless**: the JSON body written before the fatal is accurate (`{"processed":1,"delivered":1,"failed":0,...}`) and the outbox row reaches `delivered_at` at that same instant, every time. Per the coordinator's review, this scenario no longer trusts the HTTP status at all - `sc_drain_body_ok()` (drivers/stock-control.sh) asserts POSITIVELY on the JSON body's own `processed` field before proceeding, and would fail the run loudly if the body ever stopped parsing (a genuinely broken drain), rather than reading a 500 as success by default. No hop in this report is computed from `drain_http_status` or from the cron controller's HTTP response at all - every hop is computed from PrestaShop's own `ps_openlinker_webhook_outbox.delivered_at` column and from OpenLinker's own `webhook_deliveries`/`sync_jobs`/`inventory_items` timestamps. `drain_http_status` is still recorded verbatim in `cycles.csv` as a data point. The missing `exit;` is a real, fixable module defect worth its own issue independent of this measurement programme.

## Per-hop latency (measured, ms; sample sizes stated per row)

**None of these is a "reached the marketplace" figure.** The chain measured stops at the point where OpenLinker DISPATCHES a `marketplace.offerQuantity.update` job - see the Hop 5 section below for why the write itself is unmeasurable on this stand, and see the Headline Finding above for why Hop B is an excluded floor, not a shop-cron estimate.

```
total rows: 18
rows with NO outbox row at all (guard/dedup swallowed the write, or hook did not fire): 0 / 18
rows EXCLUDED from Hop F / enqueue-total (offer_children_count != 2, a window-overlap artifact - see below): 2 / 18
  product=25 cycle=1 offer_children_count=4
  product=23 cycle=2 offer_children_count=4

Hop A  write (StockAvailable::setQuantity) -> outbox row inserted (same PHP call): n=18 min=32ms p50=42ms max=54ms p95=51ms
Hop B  outbox created -> outbox delivered (cron-drain call, harness-controlled cadence): n=18 min=-771ms p50=-366ms max=-72ms p95=-230ms
Hop C  outbox delivered -> OL commits webhook_deliveries+sync_jobs (#2280 gate): n=18 min=227ms p50=526ms max=935ms p95=897ms
Hop D  OL commit -> inventory_items updated (job pickup + PS webservice re-read + write): n=18 min=2211ms p50=3648ms max=7722ms p95=4705ms
Hop E  inventory_items updated -> inventory.propagateToMarketplaces enqueued: n=18 min=-10ms p50=328ms max=5065ms p95=4996ms
Hop F  propagate enqueued -> marketplace.offerQuantity.update child enqueued: n=16 min=5014ms p50=5016ms max=5097ms p95=5095ms

TOTAL  stock write -> landed in OL's own inventory_items: n=18 min=2378ms p50=3842ms max=7915ms p95=4877ms
TOTAL  stock write -> marketplace.offerQuantity.update child DISPATCHED (NOT delivered - see Hop 5 section): n=16 min=8441ms p50=8960ms max=14106ms p95=13946ms

inventory_items rows touched per event: n=18 min=1 max=3 (1 for simple products, up to the combination count for 22/23/24)
offerQuantity.update children dispatched per event: n=18 min=2 max=4 (2 rows show 4 - the excluded window-overlap artifact above)
master.inventory.syncByExternalId terminal status distribution: {'succeeded': 18}
marketplace.offerQuantity.update lastError distribution (destination hop - never counted as a latency):
  15x: Marketplace offer quantity update failed: Connection b20b8275-905a-4413-bcc7-9661726f24cd has capability OfferManager disabled (adapter allegro.publicapi.v1 supports it)
```

**Hop B reads consistently NEGATIVE (roughly -70ms to -770ms)**, which is physically impossible for a real delivery-before-write and is reported as such rather than smoothed over: `t1` is a `microtime(true)` read inside the PrestaShop container's PHP CLI process, and `delivered_at` is a MySQL `NOW()` read inside a separate `lab-mysql` container - both should share the same host clock under ordinary Docker, but this stand runs under Docker Desktop on WSL2, where a few hundred ms of inter-container clock skew is plausible. Read as: **Hop B is effectively instantaneous / sub-second**, not literally negative-latency; the true value cannot be resolved finer than this stand's own clock-skew noise floor.

**Hop F clusters extremely tightly (5014-5097ms, n=16) rather than near-zero** - a real, reproducible ~5-second delay between the propagation job succeeding and its (permanently-failing) `marketplace.offerQuantity.update` child actually landing a row in `sync_jobs`, consistent enough to look like a fixed characteristic rather than random contention. This report does not explain it further (see "What this did not establish").

## A methodology bug found and fixed mid-run: writing `id_product_attribute=0` on a product WITH combinations silently changes nothing

An earlier draft of this run wrote every product at `id_product_attribute=0`, uniformly. For the three simple products (20/21/25) that IS the product's only stock position, but for the three products WITH combinations (22/23/24), PrestaShop treats that row as an AGGREGATE it recomputes from the combinations - `PrestashopInventoryMasterAdapter.listInventory` says so directly in its own source: "the id_product_attribute=0 aggregate is ignored - the per-combination rows [are read instead]" (`prestashop-inventory-master.adapter.ts:291-295`), and a separate comment on the SAME file states a direct write there "would be discarded" (`:1011-1012`). So a write to attribute 0 on those three products fired the webhook and re-synced the product (`inv_job outcome: ok`, looking perfectly healthy) while touching NO position OpenLinker's InventoryMaster read actually consults. On the FIRST cycle propagation still fired for every position (every `inventory_items` row was a fresh insert, which the no-change guard always treats as a change); from the SECOND cycle onward, the three unaffected combination products stopped propagating entirely - not a lane-contention stall, a wrong write target. **Fixed** by writing products 22/23/24 at one of their real combination attribute ids instead (22->40, 23->43, 24->45, `product_attr_for()` in this script) - this run's own numbers reflect the fix, and a comment above `product_attr_for()` records the finding for whoever edits this scenario next.

## Enqueue amplification (measured, after the fix above)

- One webhook event per stock-changed **product** (the hook always attributes the event to the product id - `openlinker.php`, "Always use product ID as externalId"), one `master.inventory.syncByExternalId` job per event, one `inventory_items` row upserted per non-stale `ProductVariant` under the product that GENUINELY changed value (`inventory_items_updated_count` in `cycles.csv`).
- Per changed VARIANT, one `inventory.propagateToMarketplaces` job, which dispatched exactly **2** `marketplace.offerQuantity.update` children (one per Allegro connection) - never 36 (18 synthetic Offer mappings x 2 connections that this stand's bootstrap seed data maps onto every variant, purely so the F1 order-synthesis stub has enough distinct external offer ids to reference), because the enqueue idempotency key (`inventory:{connectionId}:{productId}:{variantId}:{quantity}:{observedAt}`, `inventory-propagate-to-marketplaces.handler.ts`) omits the target offer id and collapses every mapping sharing connection+variant+quantity+timestamp onto one winning enqueue. One variant : one live offer per connection is the guarded production norm (#1837), so the 18->1 collapse is an artifact of reusing F1's seed data for a question it was not built to answer, not a newly-discovered defect.

## Located vs. pooled position shapes

Not applicable on this stand as configured: PrestaShop reports no location dimension for any of the six products (`ps_stock_available.location` is empty on every row, and the adapter never populates `Inventory.locationId` for it), so every position OpenLinker holds for this master is POOLED (`locationId IS NULL`) by construction - there is no #2324/#2325 located-position collapse to observe here, and none is claimed.

## Hop 5 (destination write) - NOT MEASURABLE on this stand, and what would actually be needed

Both Allegro connections (`perf-allegro-a`/`perf-allegro-b`) have `OfferManager` DISABLED by `bootstrap.sh`'s own design - deliberately, for a DIFFERENT scenario's (F1) benefit: that file's own comment states adding `OfferManager` would arm `marketplace.offers.sync`, whose task declares no required capability and would burn ten retry attempts against no live endpoint. More decisively, **there is no marketplace endpoint on this stand at all**: the #2856 Allegro stub (PR #2876) lives on a separate, not-yet-merged branch and is not present in this working tree or this compose file - no `allegro-stub` hostname resolves anywhere on this stand's docker network (`getent hosts allegro-stub` inside the worker container: not found). Enabling `OfferManager` here would only trade one deterministic non-write failure (capability disabled, fails in milliseconds) for another (connection refused / DNS resolution failure) - neither is a marketplace WRITE, so flipping the flag would not have produced a real number, only a slower fake one, on a connection shared with other scenarios, for no measurement gain.

**What t5 actually needs, named as a work item rather than left as a silence**: (1) the #2876 Allegro stub running and reachable on the `lab` network, (2) `perf-allegro-b` (or a dedicated third connection) repointed at it via `config.apiBaseUrl`, (3) `OfferManager` enabled on that connection, and (4) the scheduler kept OFF throughout so `marketplace.offers.sync`'s retry-burn (the exact risk `bootstrap.sh`'s comment warns about) cannot fire mid-measurement. Until all four are true, every "Hop F" / "TOTAL ... child dispatched" figure in this report means exactly that a job was handed to the retry queue toward the marketplace - never that the marketplace was updated - and the report's own hop labels say "dispatched"/"enqueued", never "delivered", for this reason.

## What this did not establish

- **No hop-5 (destination) latency, and none is claimed** - reachability of an Allegro-side write is a prerequisite this stand does not currently supply (no `allegro-stub` host, capability deliberately left disabled - see the Hop 5 section, which also names what a future run needs).
- **No production-representative t1->t2 (shop cron cadence) figure - by deliberate exclusion, not by omission.** See the Headline Finding: this run forces an immediate drain specifically to isolate OpenLinker's own work, and a real deployment must ADD its own external cron interval on top of every total this report quotes. What IS established is that the interval is unbounded absent an external caller on a stand shaped like this one, and that it would collapse toward zero automatically on a php-fpm deployment via the #2624 fast path - both operationally actionable facts, neither a production interval.
- Only 6 distinct products exist in this catalogue, so the dedup-collapse mechanism (root-caused above) caps the number of INDEPENDENT per-product outbox chains available on this stand at 6 per drain cycle - repeated over 3 cycles for 18 total attempts, never a larger independent N.
- No sustained-load / concurrent-write figure: every cycle in this run drains after a single write, so nothing here characterises what happens under a BURST of writes across many products landing between drains. A related, real observation an EARLIER, discarded draft of this run surfaced and this final run's own bounded queries still show faint traces of (the 2 excluded rows above): under several back-to-back cycles, a multi-variant product's `inventory.propagateToMarketplaces` jobs, and the resulting `marketplace.offerQuantity.update` children, can queue for long enough that a per-cycle time window of a few tens of seconds is not always enough to attribute children to the correct cycle unambiguously - a real signal about this path's behaviour under sustained sequential load, distinct from the attribute-0 methodology bug, and worth a dedicated sustained-load scenario rather than this one's single-write-per-cycle shape.
- **The ~5-second Hop F clustering is reported, not explained.** It is too tight and too consistent (a ~80ms spread across 16 samples) to read as ordinary scheduling noise, but this run does not identify its source.
- No multi-replica or multi-location figure (single worker replica on this stand; PrestaShop reports no location dimension at all, see above).

## Corrections applied after generation (for the record)

This file was regenerated from the same `cycles.csv` `run1788652154` produced, after two defects were found in the summarizer/scenario rather than in the measured system:

1. **A timezone bug in `f2-summarize.py`'s Postgres-timestamp parser** produced Hop D/E/TOTAL figures off by exactly 7,200,000 ms (2 hours) in the first generation of this file - `inventory_items.updatedAt` is Postgres `timestamp WITHOUT time zone` (confirmed via `\d inventory_items`), and a naive Python `datetime` interprets as the INTERPRETING PROCESS's own local timezone (CEST, UTC+2, on this host) rather than the column's actual UTC content. Fixed by explicitly stamping a naive Postgres timestamp UTC on parse; MySQL-sourced timestamps were unaffected (already handled correctly).
2. **A window-overlap artifact in the scenario's own bounded SQL queries** let 2 of 18 rows' `offer_children_count` double-count a neighbouring cycle's children under fast back-to-back cycles (traced to specific, named rows above, and to the sustained-load observation in "What this did not establish"). Handled by excluding those 2 rows from Hop F / the enqueue-total rather than by re-running, since the underlying data for every other hop was unaffected and this scenario had already spent its allotted stand time once.

No `cycles.csv` data was altered; only the summarizer's arithmetic and this file's prose changed.
