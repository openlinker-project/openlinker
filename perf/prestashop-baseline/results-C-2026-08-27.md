# Results C - epic #2590 verified against the pre-change baseline (#2625)

Run 2026-08-27/28 on `epic/2590-prestashop-no-module` @ `8e6b2081d` (PR #2627, NOT merged).
Instrument, catalogue and repeat counts are the ones `results-A` / `results-B` used, so the
three columns are comparable. Everything here is counted in **PrestaShop's own Apache access
log**, never by an OpenLinker-side counter.

## The stand

| | |
|---|---|
| Machine | one host, 28 logical CPUs, 15 GB RAM, Docker on WSL2 |
| **Worker replica count** | **1** (single `ol-perf-worker` process) |
| Worker role | `OL_WORKER_ROLE=jobs` - no cron tick can enqueue into a measured window |
| Lane caps in force | `realtime=4/2 bulk=12/8 fiscal=2/1 fan-out=8/4` (epic defaults) |
| Catalogue | 10 000 `PERFBASE-` products x 3 combinations, 30 008 combinations |
| PrestaShop | `ol-demo-fresh-prestashop`, localhost only, no tunnel, no VPN |
| Worker log level | lowered to `['error','warn','log']` in the perf container's compiled copy |

**Lane caps bound one worker PROCESS.** Every cap figure below is per process; N replicas
multiply each effective cap by N. This run used one replica, so process and deployment
figures coincide here and nowhere else.

## Method

- Median of runs 2-4 of four runs. Run 1 is discarded: on a cold run the identifier mapping
  does not exist yet, so the sweep takes the create branch and reads more.
- `attempts_delta` checked after every run. Zero retries in every reported run.
- Contamination verified against each run's OWN window from `sync_jobs.updatedAt`, not the
  harness's 90-minute `createdAt` window (see *Harness corrections*).
- Figures labelled **measured**, **derived** (arithmetic on a measured window) or
  **extrapolated** (a full-cycle figure reached from one tick).

## Harness corrections made for this run

These are test-code fixes against renamed product code, not product changes.

| Fix | Why |
|---|---|
| `run-a1a.sh` / `run-a3.sh` child-count queries now name `master.product.syncBatch`, `master.product.syncFromSweep`, `master.inventory.syncFromSweep` | #2593/#2594 renamed the sweep children. The old queries named only the retired types and reported 0. |
| Per-SKU denominator is the sum of `payloadJson->'externalIds'` lengths, not the child count | One batch child now carries a PAGE of 100 ids. Counting children divides by 5 where 500 products were read. |
| Run-scoped windows (`to_timestamp($MARK)`) replace `now() - interval '90 minutes'` | A 90-minute window accumulates every earlier run in the same campaign, multiplying the denominator by the run number. |
| `seed-products.sh` gained `SEED_OFFSET` | A second generation at offset 0 would mint duplicate `reference` AND duplicate `ean13`, and a duplicate barcode is what offer linking refuses to resolve. |

## A1a - requests per SKU, catalogue sweep

One product sweep tick now covers **500** products in **five** `master.product.syncBatch`
children of 100 ids each (#2593), where the baseline tick covered 100 products in 100
per-product children. Figures are normalised per 100 products so the columns compare.

### Set 1 - the stand exactly as `results-A` / `results-B` found it

| run | elapsed | requests | statuses | attempts_delta |
|---|---:|---:|---|---:|
| 1 (cold, discarded) | 1038 s | 1033 | `{200: 33, 401: 1000}` | 0 |
| 2 | 1035 s | 1025 | `{200: 29, 401: 996}` | 0 |
| 3 | 1040 s | 1025 | `{200: 29, 401: 996}` | 0 |

Runs 2 and 3 are identical to the request. **Median of runs 2-3** (the method `results-A`
used): **1025 requests, 1037.5 s, for 500 products.**

| per 100 products | baseline (A) | after adapter fix (B) | **epic (C, set 1)** |
|---|---:|---:|---:|
| requests | 796 | 397 | **205** |
| requests per SKU | 7.96 | 3.97 | **2.05** |
| elapsed | 977 s | 471 s | **207.5 s** |

Measured. 4.7x fewer requests and 4.7x less wall clock than the baseline; 1.9x fewer
requests than after the adapter fix alone.

### But 996 of those 1025 requests are HTTP 401

They are the two reads `results-A` tabulates as legitimate cost - `GET /api/product_options`
at 0.98/SKU and `GET /api/product_option_values` at 0.98/SKU. Cause verified in the shop's
own database, not inferred: the webservice key grants 25 resources and neither of those is
among them. Five resources are referenced in adapter code without a GET grant here
(`categories`, `product_features`, `product_feature_values`, `product_options`,
`product_option_values`); only the last two are on the sweep path.

So the honest reading of the comparison is two-layered:

| per SKU | baseline | after adapter fix | **epic** |
|---|---:|---:|---:|
| total requests | 7.96 | 3.97 | **2.05** |
| of which failing 401 | ~1.96 | ~1.96 | **~1.99** |
| **productive requests** | **~6.00** | **~2.01** | **~0.058** |

29 successful requests for 500 products: 19 `combinations` + 10 `products`. That is
#2593's page read doing exactly what it was designed to do, and it is a ~34x reduction
in productive requests per SKU against the adapter fix alone.

### Why one missing permission costs two requests per SKU forever

`PrestashopAttributeResolver.getOptionValueMap` writes `this.cache.set(...)` only after
both reads return, so a throw caches nothing and the next product re-reads. Its caller
`PrestashopProductMasterAdapter.buildOptionValueResolver` catches and warns - "a transient
options failure must not break variant sync". The assumption is *transient*; a missing
permission is permanent, so the cost recurs per product indefinitely. Observable but never
aggregated: **1134** identical warnings in one 40-minute window, each naming the URL.

The exception is `PrestashopAuthenticationException`, the class ADR-008 routes to the
auth-failure classifier to flip a connection to `needs_reauth`. It is caught before it gets
there, so the connection stayed `active` throughout. That is defensible - a per-resource
permission gap is not an expired credential, and flipping would stop all syncing - but it
means a genuine 401 on this path reaches no operator-facing state.

**The epic already shipped the correct pattern next door.** Its own new
`prestashop-pack.resolver.ts` (#2598) catches, and caches a NEGATIVE result under a
separate `UNRESOLVED_CACHE_TTL_MS` of 60 s, so a failure costs one read per minute per
connection rather than one per product. `prestashop-attribute.resolver.ts` and
`prestashop-feature.resolver.ts` are both still on the old shape. Pre-existing defect,
reported because it dominates the measurement the epic is judged by, and because the fix
now has an in-tree precedent.

### The elapsed figures are the rate limiter, not adapter work

Every set-1 run sat flat at 60-61 requests per minute for its whole 17 minutes
(`peak_per_minute=61 median_per_minute=60`). 1025 / 60 = 17.1 min = the measured elapsed.
The manifest declares `defaultRateLimit: { requestsPerMinute: 60 }` and the connection
carries no override, so the limiter meters every run - and ~97% of what it metered was 401.
The elapsed columns in `results-A` / `results-B` are dominated by the same waste.

### Set 2 - the same code with the two missing permissions granted

Granted GET on `product_options` and `product_option_values` for the stand's key
(`granted_now=2`), verified by direct probe: both answer 200. Effective immediately,
no PrestaShop cache clear needed. Nothing else changed.

| run | elapsed | requests | products | statuses | attempts_delta |
|---|---:|---:|---:|---|---:|
| 1 (cold, discarded) | 46 s | 29 | 500 | `{200: 29}` | 0 |
| 2 | 46 s | 29 | 500 | `{200: 29}` | 0 |
| 3 | 46 s | 29 | 500 | `{200: 29}` | 0 |
| 4 | 46 s | 29 | 500 | `{200: 29}` | 0 |

All four runs identical to the request and to the second. **Median of runs 2-4:
29 requests, 46 s, 500 products.**

| per 100 products | baseline (A) | adapter fix (B) | epic set 1 | **epic set 2** |
|---|---:|---:|---:|---:|
| requests | 796 | 397 | 205 | **5.8** |
| requests per SKU | 7.96 | 3.97 | 2.05 | **0.058** |
| elapsed | 977 s | 471 s | 207.5 s | **9.2 s** |

Measured. **Productive requests per SKU fall 6.00 -> 0.058, a ~103x reduction**, and the
sweep stops being rate-limit bound: 29 requests in 46 s is ~38/min against the declared
60 ceiling, so it is now job-bound.

The grant is a fix to the STAND, not the epic's doing. What the epic did is make the
productive cost 29 requests where the baseline needed ~600 for the same 500 products.

Where the 29 go, per 500-product tick: 19 `GET /api/combinations` + 10 `GET /api/products`.
Zero `product_options` - once the read succeeds, the process-scoped resolver caches the
dictionary for 24 h, so a full tick pays nothing for it.

**A refinement to the epic body's own figure.** The body cites "3 requests, 0.38 s,
1.33 MB for 100 fully-hydrated products" for `getProducts`. That is accurate for
`getProducts` alone; the real per-page cost is **5.8 requests** - 2 `products` plus ~3.8
`combinations` from the separate `prefetchCombinations` read. Not a contradiction, but the
quotable per-page number is 5.8, not 3.

## The throughput headline needs two changes; one shipped as a default

The epic body: "With the correct cap raised: ~277 req/min and a p95 ratio of 0.995 ...
Applied to the catalogue: ~2.4 h instead of 26.5 h."

Read from the branch and from the database:

| | |
|---|---|
| `prestashop-plugin.ts:84` | `defaultRateLimit: { requestsPerMinute: 60, maxConcurrent: 4 }`, and its own comment calls these "Placeholder values pending the reporter's actual abuse-notice text (see #1810's own open question)" |
| connection `config->>'rateLimit'` | `(none)` |
| worker boot, lane caps in force | `realtime=4/2 bulk=12/8 fiscal=2/1 fan-out=8/4` |

**Measured, not argued:** all three set-1 runs sat flat at 60-61 requests/minute for
17 minutes each, with the epic's raised `bulk=12/8` cap in force and five batch children
genuinely running concurrently. Raising the lane cap changed how many OL jobs run at
once. It did not change how many requests per minute reached the shop, because the
limiter did.

The epic's body already says the connection limit alone achieved almost nothing
(50 -> 63 req/min) and that the lane cap was the real ceiling. Both statements are true
and they concern DIFFERENT ceilings; 277 req/min needed both raised. Only the lane cap
shipped as a default, so **an operator who deploys this epic and changes nothing gets
the request-count reduction in full and none of the throughput headline.**

That does not diminish the request-count result, which is independent of the limiter -
fewer requests through the same 60/min pipe is still a shorter cycle, and set 2 shows the
sweep dropping below the ceiling entirely. It does mean "~2.4 h instead of 26.5 h" must
be quoted with the configuration it requires.

## The Redis rate limiter degraded 85 times, and pacing held anyway

Worker warnings in one 40-minute window (read with an epoch `--since`; a bare `--since`
is interpreted in the DAEMON's local time and this container's clock runs behind the host):

| count | warning |
|---:|---|
| 1134 | `Failed to resolve PrestaShop option values ... Invalid API key for .../product_options` |
| 47 | Redis rate-limiter call `claimConcurrency` timed out after 1000ms |
| 32 | Redis rate-limiter call `checkPace` timed out after 1000ms |
| 6 | Redis rate-limiter call `release` timed out after 1000ms |

Not Redis: `redis-cli --latency` reports 0 ms, and both limiter keys are tiny STRINGs.
`DBSIZE` is 37 590 but 37 500+ of those are `jobdedup:*` on ADR-049's 7-day TTL
(30 431 for master sweeps alone), and a per-connection string key is unaffected by them.
The likely cause is the worker's own event loop: five concurrent batch children each
hydrate and JSON-parse a `display=full` page (~1.33 MB), and a blocked loop cannot
process a Redis reply inside the 1000 ms budget. That is a consequence of #2594's raised
`bulk` cap, so it belongs to #2644 bound 4.

**What saved it is by design.** `RedisRateLimiterAdapter` fails DEGRADED, not open: on a
Redis error it delegates the call to a per-process insurance limiter instead of admitting
unconditionally, and tracks the delegated call in its own counters so an operator's panel
does not read 0 in-flight during the episode. That is why the observed rate stayed flat
at 60-61/min through 85 timeouts.

**Caveat that follows, and this run could not test it:** the insurance limiter is PER
PROCESS. With N worker replicas a degraded episode means pacing is enforced per process
rather than across the fleet, so shop-side rate during degradation can approach N x the
declared limit. This run used ONE replica.
