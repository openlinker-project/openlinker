# Stage B — the same measurements, after the adapter fix (#2489)

Measured on branch `2489-etap0`, cut from the stage-A baseline. The warning
that branch carried - do not merge the code before the numbers are read,
because merging it destroys what baseline A means - is spent: the numbers are
below, and this report is the record of them. The adapter change itself lands
as its own child of epic #2590.

Same catalogue (10 000 products / 30 000 combinations), same instrument (the
shop's own Apache access log), same method, same repeat counts. The only
difference is the code.

---

## What changed

### B1.1 — the adapter factory stops being rebuilt on every capability resolution

`prestashop-plugin.ts` constructed `new PrestashopAdapterFactory(...)` inside
`createCapabilityAdapter`, i.e. once per resolved capability. The factory's
resolver fields each carry a per-connection cache, and each says in its own
docblock that it is a *"process-singleton"* so that cache survives the adapter
instances the master sweep builds per product. It never was one, so the code
described a lifetime it did not have.

The factory is now built once in the plugin closure. This is safe because it
holds no per-connection state: its constructor takes only connection-independent
collaborators, and the connection is a parameter of `createAdapters`, never a
field.

### B1.2 — `GET /api/products/{id}` is fetched once per adapter instance

`getProduct` (`:187`), `getProductVariants` (`:395`) and `getProductCategories`
(`:699`) each fetched the same resource independently. A per-instance memo now
serves the second and third from the first. The **promise** is cached, not its
value, so two concurrent callers share one request; a rejection is evicted so a
transient failure does not pin an error for the rest of the job.

### B1.3 — `config.currency`: not needed

Planned as a belt to B1.1's braces. B1.1 removed the currency reads entirely
(0 remaining, see below), so it was not applied — a configuration change that
buys nothing is not worth the drift.

### Dropped from the plan, on stage-A evidence

Caching the VAT rate by tax group. Stage A measured **zero** `/api/taxes` and
`/api/tax_rules` requests across six catalogue and inventory sweep runs; they
appear only on the order path. It is not a catalogue cost, so it does not belong
in a change set justified by catalogue measurements.

---

## A1a — catalogue sweep, before and after

| Run | Requests | Products | Per SKU | Elapsed |
|---|---:|---:|---:|---:|
| B run 1 (cold, discarded) | 502 | 100 | 5.02 | 590 s |
| B run 2 | 496 | 100 | 4.96 | 592 s |
| B run 3 | 496 | 100 | 4.96 | 587 s |
| **B median (2, 3)** | **496** | **100** | **4.96** | **~590 s** |

### Per-request comparison

| Request | Baseline A | Baseline B | Change |
|---|---:|---:|---|
| `GET /api/products/<id>` | 3.00 | **1.99** | −34% |
| `GET /api/configurations` | 1.00 | **0.00** | **gone** |
| `GET /api/currencies/<id>` | 1.00 | **0.00** | **gone** |
| `GET /api/combinations` | 1.00 | 1.00 | — |
| `GET /api/product_options` | 0.98 | 0.98 | — |
| `GET /api/product_option_values` | 0.98 | 0.98 | — |
| **total per SKU** | **7.96** | **4.96** | **−37.7%** |

### The memo collapsed three fetches to two, not to one

Measured: 199 fetches across 100 distinct products, i.e. **1.99 per product**,
where 1.00 was expected.

The reason is a lifetime, not a bug in the memo: an adapter instance lives for
one *capability resolution*, and a child job resolves `ProductMaster` more than
once. Two of the three reads share an instance and hit the memo; the third
lands on a freshly built instance and misses.

**The remaining 0.99 fetches per SKU is the measured size of the next step:**
memoising the adapter bundle itself per connection. That is a real lifetime
change with real consequences (credential rotation, config edits mid-run) and
belongs in its own decision, not smuggled into this one.

## A3 — inventory sweep, before and after

| Run | Requests | Positions | Per position | Elapsed |
|---|---:|---:|---:|---:|
| B run 1 | 100 | 100 | 1.00 | 165 s |
| B run 2 | 100 | 100 | 1.00 | 160 s |
| B run 3 | 100 | 100 | 1.00 | 164 s |

| Request | Baseline A | Baseline B |
|---|---:|---:|
| `GET /api/stock_availables` | 1.00 | 1.00 |
| `GET /api/configurations` | 1.00 | **0.00** |
| `GET /api/currencies/<id>` | 1.00 | **0.00** |
| **total per position** | **3.00** | **1.00** |

**−66.7%.** The inventory sweep was two-thirds overhead; the useful request is
now the only request. Elapsed per 100 positions fell from ~404 s to ~163 s.

The `throwForAbsentStockRecords` probe is untouched and still costs nothing in
the steady state — zero `GET /api/products/<id>` observed here too.

## A2 — storefront, before and after

| | p95 idle | p95 under sweep | ratio |
|---|---:|---:|---:|
| Baseline A | 0.0386 s | 0.0382 s | **0.989** |
| Baseline B | 0.0378 s | 0.0380 s | **1.005** |

Unchanged, and expected to be: the storefront was already at parity before the
fix, so there was nothing here to improve. Both ratios sit inside the noise.

## A4 — eight-line order

**Not repeated.** It was never measured in stage A (no supported path dispatches
a synthetic order — see `results-A`), and the fix touches neither the order path
nor price pinning.

---

# The decision table

Per 100 products unless stated. Every cell in the first two columns is measured
on this stack; the cycle rows are COMPUTED from those measurements and their
inputs are shown in `results-A` § A1b.

| | Baseline A (today) | Baseline B (after fix) | Change |
|---|---:|---:|---:|
| Requests per SKU, catalogue | 7.96 | **4.96** | **−37.7%** |
| `GET /products/<id>` per product | 3.00 | 1.99 | −33.7% |
| Adapter-build pair per product | 2.00 | **0.00** | **−100%** |
| Sweep tick, 100 products | 977 s | **590 s** | −39.6% |
| Requests per stock position | 3.00 | **1.00** | **−66.7%** |
| Inventory tick, 100 positions | 404 s | **163 s** | −59.7% |
| Storefront p95 under load ÷ idle | 0.989 | 1.005 | — |
| *COMPUTED* 10 000-SKU catalogue cycle | ~26.5 h | **~16.5 h** | −37.7% |
| *COMPUTED* 10 000-position inventory cycle | ~11.2 h | **~4.5 h** | −59.7% |

Cross-check on the catalogue cycle: 4.96 × 10 000 ÷ 50 req/min = 992 min
(16.5 h); from elapsed, 590 s per 100 → 16.4 h. The two routes agree.

## What this cost

Two files, no new dependency, no migration, no schema change, no ADR-level
contract change:

* `libs/integrations/prestashop/src/prestashop-plugin.ts` — the factory moves
  out of a per-call scope into the plugin closure.
* `libs/integrations/prestashop/src/infrastructure/adapters/prestashop-product-master.adapter.ts`
  — one memo field and one private method; three call sites now go through it.

## What is still on the table, with its measured size

| Next step | Measured size | Cost |
|---|---|---|
| Memoise the adapter bundle per connection | the remaining **0.99** product fetches per SKU (1.99 → 1.00) | a real lifetime change: credential rotation and mid-run config edits become observable. Its own decision. |
| Wire the existing `getProducts(display=full)` into the sweep | measured bulk floor is **3 requests / 0.38 s per 100 products** vs 496 / 590 s today | `PrestashopProductMasterAdapter.getProducts` (`:305`) already exists and has exactly one caller — itself, from `searchProducts` (`:767`). Nothing in core calls it. |
| Per-line price pinning on the order path | `k + 2n`, factor read from source, not measured | needs a genuine multi-line order in the sandbox |

## Reading this against the module proposal

The two rows that matter: the catalogue sweep dropped **37.7%** and the
inventory sweep **66.7%**, from two localised changes in one adapter, with no
PrestaShop-side code at all.

The measured bulk floor — 3 requests and 0.38 s for 100 fully hydrated products,
taken through the stock PrestaShop webservice — is another two orders of
magnitude below baseline B, and the adapter method that reads it is already
written.

None of this addresses the module proposal's functional half (returns, partial
cancellation, cash on delivery, buyer tax id, whole-order discounts, bundles,
one-way multilingual) or acceptance criteria 5–7 (retry, sync status, native
data access). Those are product decisions; no stopwatch settles them.


---

# B1.4 — the third product fetch, found and removed

Baseline B left 1.99 product fetches per SKU where 1.00 was expected, and the
report above guessed the cause was the adapter bundle's lifetime. **That guess
was wrong**, and chasing it in the log rather than trusting it is what found the
real one.

`getProductCategories` — the suspected third caller — is never reached by a
catalogue sweep at all; its only caller in the tree is the shop-publish builder.
The extra fetch came from somewhere else entirely: `readProductTaxRate` is
called per product by `MasterProductSyncService` (`:270`), and
`PrestashopTaxRateResolver` fetches `products/{id}` **through its own code
path**, which the adapter's memo cannot see.

All that resolver reads off the product is `id_tax_rules_group`. It now accepts
the already-fetched product as an optional argument, and the adapter hands over
the one it just memoised. Omitting the argument keeps the previous behaviour
exactly — the resolver fetches for itself — so no other caller changes.

One supporting change: `PrestashopProduct` did not declare
`id_tax_rules_group`, though the resource carries it. It is declared now, marked
optional because PrestaShop omits it on a "No tax" product, which the resolver
already reads as a deliberate 0 rather than a gap.

| Run | Requests | Products | Per SKU | `products/<id>` per SKU | Elapsed |
|---|---:|---:|---:|---:|---:|
| C run 1 (cold, discarded) | 404 | 100 | 4.04 | 1.01 | 488 s |
| C run 2 | 397 | 100 | 3.97 | 1.00 | 465 s |
| C run 3 | 397 | 100 | 3.97 | **1.00** | 477 s |
| **C median (2, 3)** | **397** | **100** | **3.97** | **1.00** | **~471 s** |

Every product is now fetched exactly once.

A3 was not re-run: the inventory sweep was already down to its single useful
request (`GET /api/stock_availables`) at baseline B, and this change touches the
product path only.

---

# The decision table, final

| | Baseline A | +factory & memo | +tax preload | Total change |
|---|---:|---:|---:|---:|
| Requests per SKU, catalogue | 7.96 | 4.96 | **3.97** | **−50.1%** |
| `GET /products/<id>` per product | 3.00 | 1.99 | **1.00** | −66.7% |
| Adapter-build pair per product | 2.00 | 0.00 | 0.00 | −100% |
| Sweep tick, 100 products | 977 s | 590 s | **471 s** | **−51.8%** |
| Requests per stock position | 3.00 | **1.00** | 1.00 | −66.7% |
| Inventory tick, 100 positions | 404 s | **163 s** | 163 s | −59.7% |
| Storefront p95 ratio | 0.989 | 1.005 | — | unchanged |
| *COMPUTED* 10 000-SKU catalogue cycle | ~26.5 h | ~16.5 h | **~13.2 h** | **−50.1%** |
| *COMPUTED* 10 000-position inventory cycle | ~11.2 h | **~4.5 h** | ~4.5 h | −59.7% |

Cross-check on the final catalogue cycle: 3.97 × 10 000 ÷ 50 req/min = 794 min
(13.2 h); from elapsed, 471 s per 100 → 13.1 h. The two routes agree.

## The whole cost of this

Four files. No new dependency, no migration, no schema change, no change to any
cross-package contract:

* `prestashop-plugin.ts` — the factory moves out of a per-call scope.
* `prestashop-product-master.adapter.ts` — one memo field, one private method,
  four call sites through it.
* `prestashop-tax-rate.resolver.ts` — one optional parameter, defaulted to the
  previous behaviour.
* `prestashop.mapper.interface.ts` — one optional field declared on a type that
  already received it.

## What is still on the table

| Next step | Measured size | Note |
|---|---|---|
| Wire the existing `getProducts(display=full)` into the sweep | bulk floor measured at **3 requests / 0.38 s per 100 products**, against 397 / 471 s now | `PrestashopProductMasterAdapter.getProducts` (`:305`) already exists and its only caller in the entire repository is `searchProducts` in the same file (`:767`) |
| Per-line price pinning on the order path | `k + 2n`, factor read from source | needs a genuine multi-line order in the sandbox |


---

# The two prerequisites for the bulk path, verified rather than assumed

Both are named in the module proposal as small blockers. Both are real, and
both were checked here rather than taken on trust.

**1. `filter[id]` is joined with a comma, and PrestaShop reads that as a RANGE.**

`prestashop-query.builder.ts:92` does `filters.ids.map(String).join(',')`, so
three ids go out as `filter[id]=[a,b,c]`. Measured against the live shop:

| Query | Rows returned |
|---|---:|
| `filter[id]=[100002,100005,100009]` | **1** |
| `filter[id]=[100002\|100005\|100009]` | **3** |

An OR list needs pipes. A bulk read that fetches N products by id therefore
cannot use this builder as it stands.

**2. No `sort` parameter is emitted at all.** The builder's own docblock lists
"sorting" among what it handles; no `sort` appears anywhere in the file. This
is the same gap ADR-048 records for `listExternalIds`, and it is what makes a
paged sweep unstable across ticks.

Neither is changed here. They are the first step of the bulk work, which is a
separate decision, and altering filter semantics without auditing every caller
is exactly the sort of change that wants a review rather than a measurement
session. They are recorded so the estimate for that work rests on something
checked.


---

# Correctness check — the fix is a request-count change, not a data change

A cache that returns the wrong thing is worse than the requests it saves, so the
data the sweep wrote after the fix was compared with the data it wrote before.

| | Before the fix | After the fix |
|---|---:|---:|
| `PERFBASE-` products with exactly 3 variants | — | **96** |
| products with any other variant count | — | **0** |
| variant rows written | 474 | 288 |
| rows with `taxRate` set | 0 | **0** |
| rows with `taxRateUnknownReason` set | 0 | **0** |
| rows with `taxRateReadAt` set | 0 | **0** |

Every product came back with its three variants, each carrying its EAN and its
price. No product acquired or lost a variant.

The tax columns are the ones the preload could plausibly have disturbed, since
the resolver now reads `id_tax_rules_group` off a product it did not fetch
itself. They are **byte-identical before and after** — and the reason is worth
recording: the PrestaShop webservice does not return `id_tax_rules_group` at all
for these products (verified directly against the live shop), so the resolver's
own fetch was already getting nothing. Handing it the memoised product changes
which request the field is absent from, not the answer.

That the rate is unset at all is a pre-existing condition of this catalogue,
present identically before the change, and is not something this work caused or
claims to fix.


---

# Control — the improvement is the code, not the environment

Everything else moved during this campaign: a different worker container, a
purged queue, a repaired connection, a scheduler that had been switched off. Any
of it could in principle explain a 50% drop, so the baseline was re-taken on
**pristine code**.

The patched container was destroyed and a fresh one started from the unmodified
image (verified: `fetchProductResource` appears 0 times in its
`prestashop-product-master.adapter.js`), under the campaign's conditions —
same catalogue, same scheduler-free role, same cleared queue and cursor.

| | Requests | Per SKU | `products/<id>` | `configurations` | `currencies` | Elapsed |
|---|---:|---:|---:|---:|---:|---:|
| Original baseline A | 796 | 7.96 | 300 | 100 | 100 | 977 s |
| **Control, pristine code, after everything else changed** | **799** | **7.99** | **300** | **101** | **101** | **975 s** |
| After the three changes | 397 | 3.97 | 100 | 0 | 0 | 471 s |

The control reproduces the baseline to within 0.4% on requests and 0.2% on
elapsed time. Nothing about the environment moved the number; the code did.


---

# A4 on stage-0 code — the order path, measured before and after

Both runs are **fresh order creations** (a new eight-line order placed in
WooCommerce each time, a new PrestaShop order created: #11 then #12), so they
are like-for-like: customer provisioning, address creation and the module's
`importorder` call are present in both.

| Request | Baseline A | Stage 0 |
|---|---:|---:|
| `POST /api/specific_prices` | 8 | **8** |
| `DELETE /api/specific_prices/<id>` | 8 | **8** |
| `GET /api/addresses` | 2 | 2 |
| `POST /index.php` [`importorder`] | 1 | 1 |
| `POST /index.php` [`cartshipping`] | 1 | 1 |
| `POST /api/carts` | 1 | 1 |
| `POST /api/addresses` | 1 | 1 |
| `GET /api/orders` | 1 | 1 |
| `GET /api/customers` | 1 | 1 |
| `GET /api/currencies` (list) | 1 | 1 |
| `GET /api/countries` | 1 | 1 |
| `GET /api/carriers` | 1 | 1 |
| `GET /api/configurations` | 1 | **0** |
| `GET /api/currencies/<id>` | 1 | **0** |
| **total** | **29** | **27** |

**Stage 0 removes exactly 2 requests from an order, and they are the
adapter-build pair the factory hoist eliminated.** Everything else is identical.

**The sixteen price-pinning calls are untouched**, and they are now **59% of the
whole order**. That is expected and worth being precise about: the three changes
made here are a factory lifetime, a product memo and a tax preload. The order
path fetches no products, so two of the three cannot reach it; only the first
does, and it is worth 2.

Removing the per-line pinning is a **separate change on a separate path** — it
appears in the module proposal's own stage-0 list, and was not implemented here.

## What that makes the order-path decision worth, arithmetically

| Option | Requests per 8-line order | Saved |
|---|---:|---:|
| Baseline A | 29 | — |
| Stage 0 as built | 27 | 2 |
| Stage 0 + carry line prices in the module payload instead of pinning | **11** | **16** |
| A module `/orders/import` that takes the whole order in one call | **1** | **26** |

The middle row is the interesting one: it needs no new module endpoint, only
that the price the line should be sold at travels in the payload the OL module
already receives, instead of being written to `specific_prices` and deleted
again. It is 59% of the order's request cost.

## One recurring behaviour found on this path

**Every first dispatch of a fresh WooCommerce-sourced order failed on a customer
mapping conflict**, both times:

```
Mapping conflict: Customer:14@<ps-connection> is already mapped to
ol_customer_6797e2..., cannot map to ol_customer_...
```

PrestaShop creates the customer, and the mapping write then conflicts with an
internal customer resolved differently. The guard behaves correctly — it refuses
to overwrite — but the order needs a manual mapping deletion before it can be
retried. Whether this is specific to this stack's customer-identity
configuration or a genuine defect on the WooCommerce→PrestaShop path was not
established here; it is recorded because it reproduced twice out of two.


---

# A2 at raised throughput — the p95 argument no longer rests on a pre-change rate

The earlier A2 runs measured the storefront at ~50 requests/min, which is the
rate the *unfixed* code produced. Stage 0 halves the requests per SKU, so the
honest question is not "does the shop cope with 50/min" but **"can we now go
faster, and does the shop still cope"**.

## Raising the connection's rate limit does almost nothing

`config.rateLimit` was set to `{requestsPerMinute: 300, maxConcurrent: 8}`,
five times the manifest default. Measured throughput went from ~50 to **63
req/min**. The declared limit was not the binding constraint.

**The ceiling is `OL_LANE_REALTIME_SCOPE_CAP`, whose default is 2.**
`master.product.syncByExternalId` is registered in the `realtime` lane
(`handler-registration.service.ts:190`), and the per-scope cap allows two
concurrent jobs per connection regardless of what the connection's rate limit
says. Raising `fanout` and `bulk` — the obvious guess — changes nothing, because
the sweep's children are in neither.

## With the right cap raised

`OL_LANE_REALTIME_CAP=12`, `OL_LANE_REALTIME_SCOPE_CAP=12`:

| | Requests/min | p95 idle | p95 under sweep | ratio |
|---|---:|---:|---:|---:|
| Baseline A, default caps | ~50 | 0.0386 s | 0.0382 s | 0.989 |
| Stage 0, default caps | ~50 | 0.0378 s | 0.0380 s | 1.005 |
| **Stage 0, raised caps** | **~277** | 0.0405 s | 0.0403 s | **0.995** |

**At 5.5x the throughput the storefront is unchanged.** Peak observed 277
req/min against a shop that answered every storefront request in the same 40 ms
it does when idle.

## What that does to the cycle

| | Requests per SKU | Sustained rate | 10 000-SKU cycle |
|---|---:|---:|---:|
| Baseline A | 7.96 | ~50/min | ~26.5 h |
| Stage 0, default caps | 3.97 | ~50/min | ~13.2 h |
| **Stage 0, raised caps** | 3.97 | **~277/min** | **~2.4 h** (COMPUTED) |

39 700 requests at 277/min is 143 minutes. From over a day to under three hours,
with the shop showing no measurable effect.

## The caveat that belongs with it

`OL_LANE_REALTIME_SCOPE_CAP` is not a PrestaShop setting. It bounds every
`realtime` job for every connection in the worker process, and
[ADR-050](../../docs/architecture/adrs/050-workload-isolation-concurrency-lanes.md)
sizes these caps by cost-of-starvation, not by throughput. Raising it globally
to chase catalogue speed would let a catalogue sweep crowd out the very work the
`realtime` lane exists to protect — a buyer's order.

So this row is **evidence that the shop can take more, not a recommendation to
set the cap to 12**. What it establishes is that after stage 0 the constraint is
OpenLinker's own concurrency policy rather than PrestaShop's tolerance, which is
a different problem with different owners — and one the module proposal does not
address either.

