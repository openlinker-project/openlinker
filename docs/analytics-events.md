# Demo Analytics Events

This is the marketing handoff table for the demo-mode PostHog event catalog. The
source of truth is `apps/web/src/features/demo/lib/demo-events.ts` — this
document is generated by hand from that file and must be kept in sync whenever
an event is added, renamed, or removed.

**Status**: framework (#1786) + e-commerce reel batch (#1788) landed. The
remaining batches (#1789, #1790) are still to come.

Events only fire on a demo-mode build (`OL_DEMO_MODE=true`) with PostHog
configured (env vars or the `/settings` panel) **and** the visitor has
accepted the analytics-consent banner. On a normal self-hosted install, none
of this code path runs.

## Enabling event groups (#1787)

Product events are off by default even on a fully-configured PostHog
connection. An admin turns them on at `/settings` → PostHog → **Product
events**: a master toggle (independent of autocapture) plus one toggle per
event **group** — groups are derived automatically from `DemoEventCatalog`,
never hand-maintained, so a new group added to the catalog appears in the
panel with zero settings-page edits. `captureDemoEvent` is a no-op unless
both the master toggle and the specific event's group are enabled.

| Event | Description | Props | What it measures |
|---|---|---|---|
| `demo_viewer_locked_action_clicked` | Viewer clicked a locked (read-only) write action — the primary intent-to-convert signal for a read-only demo session | `actionName: string`, `surface: string` | How often, and where, a read-only demo visitor attempts the final write action that `ReadOnlyLock`/`useWriteAccess` renders as disabled — the highest-value conversion-intent signal available from a viewer-only session. |
| `demo_products_viewed` | Viewer loaded the products list | `resultCountBucket: string` (`'0'`/`'1-10'`/`'11-50'`/`'50+'`) | Top-of-funnel reach for the products cockpit. |
| `demo_product_row_expanded` | Viewer expanded a product row for more detail (mobile card view) | — | Exploration depth on the mobile card layout. |
| `demo_offer_create_launched` | Viewer launched the offer-creation flow | `source: 'row' \| 'bulk_bar'` | Which entry point (per-row vs bulk-selection) drives offer creation launches. |
| `demo_offer_marketplace_picked` | Viewer picked a marketplace connection to create an offer on | `platform: string` | Which marketplace an operator with 2+ connections chooses. |
| `demo_offer_wizard_step_advanced` | Viewer advanced a step in the offer-creation wizard | `platform: string`, `step: string` | Wizard step-by-step drop-off. |
| `demo_offer_wizard_review_reached` | Viewer reached the review step of the offer-creation wizard | `platform: string` | How many wizard sessions reach the final review step before the locked create click. |
| `demo_offer_create_attempted` ★ | Viewer clicked "Create offer(s)" — the locked write action | `platform: string`, `mode: string` | The primary offer-creation intent-to-convert signal. |
| `demo_orders_viewed` | Viewer loaded the orders list | — | Top-of-funnel reach for the orders cockpit. |
| `demo_orders_filtered` | Viewer applied a filter on the orders list | `filter: string`, `value: string` | Which order filters (health segment, SLA-breaching toggle) are used. |
| `demo_order_opened` | Viewer opened an order detail page | — | Orders-list → order-detail conversion. |
| `demo_label_form_opened` | Viewer opened the generate-shipping-label form | `entry: 'empty_state' \| 'active_shipment_row'` | Which entry point drives label-form opens. |
| `demo_label_generate_attempted` ★ | Viewer clicked "Generate label" | `carrier: string` | The primary shipment intent-to-convert signal (not currently gated by `ReadOnlyLock` — see #1788's own scope note). |
| `demo_invoice_doctype_changed` | Viewer changed the invoice document type before issuing | `documentType: string` | Document-type preference before issuing. |
| `demo_invoice_issue_attempted` ★ | Viewer clicked "Issue invoice" — the locked write action | — | The primary invoicing intent-to-convert signal. |

## Adding a new event

1. Add an entry to `DemoEventCatalog` in `apps/web/src/features/demo/lib/demo-events.ts` with a `description`, a `group`, and a typed `props` shape (bounded strings/numbers/booleans only — never PII, free text, or entity ids). The `props` value must carry real placeholder values for every key (e.g. `{ actionName: '', surface: '' } as { actionName: string; surface: string }`), **not** a bare `{}` cast — the `/settings` Product-events panel introspects prop *names* via `Object.keys(entry.props)` at runtime, and a bare `{}` silently shows "props: (none)" in that panel. `demo-events.test.ts` asserts every entry has non-empty runtime prop keys.
2. Call `captureDemoEvent(eventName, props)` (exported from the `demo` feature barrel) at the call site.
3. Add a row to the table above.
